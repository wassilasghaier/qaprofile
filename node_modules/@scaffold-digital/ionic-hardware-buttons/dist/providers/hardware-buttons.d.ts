import { App, Platform, ViewController } from 'ionic-angular';
/**
 * Defines a method that will be called when the hardware back button is pressed
 * and this view is visible. Returning false will prevent the default
 * functionality.
 */
export declare function BackButton(): (target: any, propertyKey: string, descriptor: PropertyDescriptor) => void;
export declare class HardwareButtons {
    private app;
    private platform;
    private viewStack;
    constructor(app: App, platform: Platform);
    private getCallbackForViewCtrl(viewCtrl);
    private getCurrentViewCtrl();
    init(): void;
    triggerCallbackForViewCtrl(viewCtrl: ViewController): any;
}
